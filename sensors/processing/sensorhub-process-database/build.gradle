description = 'Datasave Process'
ext.details = 'Database save processing'
version = '1.0.0'

dependencies {
  implementation 'org.sensorhub:sensorhub-core:' + oshCoreVersion
}
  
// add info to OSGi manifest
osgi {
  manifest {
    attributes('Bundle-Vendor': 'Sensia Software LLC')
    attributes('Bundle-Activator': 'org.sensorhub.process.datasave.Activator')
  }
}

// add info to maven pom
ext.pom >>= {
  developers {
    developer {
      id 'alexalmanza'
      name 'Alex Almanza'
      organization 'Botts Innovative Research'
      organizationUrl 'http://www.botts-inc.com'
    }
  }
}

